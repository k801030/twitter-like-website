/*  document  */


為了讓資料呈現更簡便，因此使用angular
angular: 花了很多時間在想如何讓資料進行更新。


思考很多方法：

1. 定時向資料庫更新（一次抓全部資料）
2. 同上（一次抓timestamp後的資料）
3. 透過server去通知新的資料。
4. server keeps 新資料，回覆要求。


Heroku:
npm init/package.json     
Procfile web: node server.js  // locally start
 use the Harp buildpack    // 可以查到dir
